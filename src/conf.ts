
const VERSION = '0.01'

const BASE = 'https://apex.str.manfulls.tech'
const API = BASE + '/api'
const MEDIA = BASE

const TEST = true
const TEST_DEV = true

export default {
    TEST,
    TEST_DEV,

    BASE, API, MEDIA,

    VERSION
}