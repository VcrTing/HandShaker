
type AII = { [k: string]: any }

type ONE = { [k: string]: any }

type ONE_STR = { [k: string]: string }

type MANY = ONE[ ]

type strings = string[]
type numbers = number[]

type ID = string | number
type IDS = string[] | number[]

type ADDRESS = { region: string, district: string, address: string }